<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>第10期 · 腾讯IEG光子一面</title>
      <link href="/2026/04/23/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F10/"/>
      <url>/2026/04/23/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F10/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>腾讯 IEG 光子 一面 4.23 11:00-12:00</strong></p></blockquote><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><h3 id="Vue-的-DIFF-算法核心是什么，具体怎么实现"><a href="#Vue-的-DIFF-算法核心是什么，具体怎么实现" class="headerlink" title="Vue 的 DIFF 算法核心是什么，具体怎么实现"></a><code>Vue</code> 的 <code>DIFF</code> 算法核心是什么，具体怎么实现</h3><p>无论是 <code>Vue2</code>、<code>Vue3</code>，还是 <code>React</code>，它们在对比两棵树时，都是“同层比较”。如果同一层级的节点类型（如 <code>div</code> 变成了 <code>span</code>）或 <code>key</code> 发生了变化，它们都会直接销毁旧节点、创建新节点，不再继续向下深度比较。</p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-15.png" alt="面经图 15"></p><h2 id="大模型"><a href="#大模型" class="headerlink" title="大模型"></a>大模型</h2><h3 id="上下文工程演变历程"><a href="#上下文工程演变历程" class="headerlink" title="上下文工程演变历程"></a>上下文工程演变历程</h3><p>上下文工程的本质是【如何更高效地管理大模型的短期显存】，主要经历了以下五个阶段：</p><p><strong>1. 意图对齐期：<code>Few-Shot</code> (少样本学习)</strong></p><p>痛点：纯零样本（<code>Zero-shot</code>）下，模型输出的格式和语气易跑偏，不符合业务预期</p><p>方案：在上下文中预置 2-3 个标准 <code>Q&amp;A</code> 样例，模型无需微调即可通过类比完成任务</p><p><strong>2. 容量突破期：<code>RAG</code> (检索增强生成)</strong></p><p>痛点：早期模型 <code>Token</code> 窗口小(4K-8K)，无法直接塞入长文档或私密数据，易产生事实幻觉</p><p>方案：引入向量数据库（<code>Vector DB</code>）。在喂给大模型前先进行语义检索，取出最相关的切片（<code>Top-K</code> <code>Chunks</code>），按需拼接进上下文</p><p><strong>3. 降本提效期：压缩与缓存 (Compression &amp; Caching)</strong></p><p>痛点：128K+ 超长窗口带来副作用：1)中间细节丢失（<code>Lost in the Middle</code>）；2)<code>Token</code> 费用高昂；3)首字响应（<code>TTFT</code>）极慢</p><p>方案：【信息压缩】引入小模型（如 <code>LLMLingua</code>）提前剔除无用停用词，实现信息浓缩。【物理缓存】利用 <code>Prompt Caching</code> 技术，将长期不变的系统提示词或长文档固化在 <code>GPU</code> 显存池中，命中后实现降本及秒级响应</p><p><strong>4. 架构隔离期：多智能体编排 (Agentic Workflow)</strong></p><p>痛点：复杂任务下，将多轮历史、旧目标、报错栈全塞入单一上下文，会导致模型逻辑错乱（即上下文污染 <code>Context Pollution</code> &#x2F; 上下文腐化 <code>Rot</code>&#x2F; 上下文衰减 <code>Decay</code>）</p><p>方案：从“单体巨石”转向“微服务”。拆分多 <code>Agent</code>，实施上下文局部隔离（例如：<code>Planner</code> 只看宏观需求，<code>Coder</code> 只看当前函数）。引入草稿纸（<code>Scratchpad</code>）机制隔离推演过程与最终输出</p><p><strong>5. 跨会话感知期：持久化记忆 (Stateful Memory)</strong></p><p>痛点：模型底层无状态（<code>Stateless</code>），跨级、跨会话的历史经验无法继承，需反复输入背景信息</p><p>方案：引入全自动化记忆层。在后台静默提取用户偏好构建【特征图谱】，当用户开启新会话时，系统利用语义检索精准激活历史记忆，前置拼装为用户的上下文</p><h3 id="skill-是直接传给底层大模型嘛"><a href="#skill-是直接传给底层大模型嘛" class="headerlink" title="skill 是直接传给底层大模型嘛"></a><code>skill</code> 是直接传给底层大模型嘛</h3><p><strong>不会直接生传，通常经过如下中间件：</strong></p><p>意图路由引擎：判断当前任务是否需要使用特定的 <code>skill</code></p><p>文本解析与 <code>Chunking</code>：如果 <code>Markdown</code> 内容长，会切片或者转换为结构化提示</p><p><code>RAG</code> 向量检索：如果是知识型 <code>skill</code>，会被作为外部知识存入向量库检索</p><p><code>Prompt Builder</code>（拼装器）：将 <code>user query</code>, <code>rules</code>（系统规则）, <code>history</code>（记忆）和截取的 <code>skill.md</code> 内容动态组装进最终的 <code>Prompt</code> <code>payload</code> 中，提交给 <code>LLM</code></p><h3 id="AI-与人如何协作"><a href="#AI-与人如何协作" class="headerlink" title="AI 与人如何协作"></a><code>AI</code> 与人如何协作</h3><p><strong>人做架构与审查，<code>AI</code> 做填空与执行（<code>Copilot</code> 模式）</strong></p><p><strong><code>AI</code> 负责：</strong> 繁琐样板代码生成、文档编写、单测补充、海量日志初步排错</p><p><strong>人类负责：</strong> 核心业务逻辑设计、边界条件定义、架构选型、最终 <code>Safety Review</code></p><p><strong>信任但验证（<code>Trust but Verify</code>）：</strong> 将 <code>AI</code> 视为【高智商但容易粗心的应届生实习生】</p><h3 id="AI-时代怎么学习知识，怎么学会知识"><a href="#AI-时代怎么学习知识，怎么学会知识" class="headerlink" title="AI 时代怎么学习知识，怎么学会知识"></a><code>AI</code> 时代怎么学习知识，怎么学会知识</h3><p>这个问题很诡异，我说通过与 <code>AI</code> 对话快速获取知识，面试官说【<code>AI</code> 像是老师，老师讲完之后你怎么学习并把知识化为自己的】，那我就说可能通过 <code>NotebookLM</code> 之类的工具提高做笔记的效率，不确定是否回答到点子上……</p><h3 id="Rules、Memory-和-Harness-的区别"><a href="#Rules、Memory-和-Harness-的区别" class="headerlink" title="Rules、Memory 和 Harness 的区别"></a><code>Rules</code>、<code>Memory</code> 和 <code>Harness</code> 的区别</h3><p><code>Rules</code> 是指导大模型行为的系统级约束与准则（<code>System Prompt</code>），核心作用是限制行为边界和指导输出风格。不会因为你当前在开发什么项目而改变，规定了 <code>AI</code>【能做什么、不能做什么、以什么语气做】</p><p>设定 <code>AI</code> 必须使用 <code>TypeScript</code> 强类型，禁止使用 <code>any</code> 标注；要求所有 <code>UI</code> 组件必须优先使用 <code>Tailwind CSS</code> 类名而不是内联样式；规定生成的代码中必须要包含 <code>JSDoc</code> 注释等。这些属于强制遵守的法则</p><p><code>Memory</code> 是 <code>AI</code> 对当前环境的认知和历史记录，它的核心作用是提供上下文，解决【我是谁、我在哪、我在做什么】的问题。<code>Memory</code> 是动态的，随着你的操作不断更新</p><p>实例：</p><p>短期记忆包含你和 <code>AI</code> 的前 10 轮对话历史，以及当前编辑器里高亮选中的那十几行代码</p><p>长期记忆（<code>RAG</code> 知识库）则是 <code>Cursor</code> 在底层把你整个项目的万行代码、路由结构解析后存入的本地向量数据库</p><p><code>Harness</code> 是隐藏在模型背后的工程化执行与验证外壳</p><p>实例：</p><p>当大模型生成一段代码后，<code>Harness</code> 会在后台利用 <code>Language Server Protocol</code>（<code>LSP</code>）悄悄进行语法检查，如果发现有类型未引入或拼写错误，<code>Harness</code> 会自动把错误信息发回给大模型让它重新修改，然后再把最终正确的结果展示给你看（这就是 <code>Shadow Workspace</code> 机制）。此外，帮你自动执行终端命令、一键应用 <code>Diff</code> 代码、分析你的项目目录树结构再传给模型，这些将“模型文本”转化为“编辑器实际动作”的基础设施系统，全部统称为 <code>Harness</code></p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-16.jpg" alt="面经图 16"><br><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-17.jpg" alt="面经图 17"></p><h2 id="工程"><a href="#工程" class="headerlink" title="工程"></a>工程</h2><h3 id="资源包更新怎么提高效率（只有部分文件发生改变）"><a href="#资源包更新怎么提高效率（只有部分文件发生改变）" class="headerlink" title="资源包更新怎么提高效率（只有部分文件发生改变）"></a>资源包更新怎么提高效率（只有部分文件发生改变）</h3><p><strong>内容哈希（<code>Content Hash</code>）：</strong> <code>Webpack</code> &#x2F; <code>Vite</code> 打包时给文件名带上 <code>Hash</code>（如 <code>app.a1b2.js</code>）。只更新变动的文件，未变动的文件享受浏览器强缓存</p><p><strong>拆包（<code>Code Splitting</code>）：</strong> 将稳定的第三方库（<code>Vendor</code>）和业务代码分离，业务更新不影响依赖库缓存</p><p><strong>差异化更新（增量更新 &#x2F; <code>BSDiff</code>）：</strong> 移动端&#x2F;桌面端常见，下发 <code>patch</code> 补丁包，客户端合并新旧版本</p><p><strong><code>Service Worker</code> 预缓存：</strong> 结合 <code>PWA</code>（渐进式 <code>Web</code> 应用，<code>Progressive Web App</code>）策略，后台静默下载变动文件，下次打开秒开</p><h3 id="出现偶发问题怎么排查-移动端"><a href="#出现偶发问题怎么排查-移动端" class="headerlink" title="出现偶发问题怎么排查(移动端)"></a>出现偶发问题怎么排查(移动端)</h3><p><strong>留痕与上报：</strong> 接入自研日志系统，捕获报错堆栈、<code>SourceMap</code> 还原，记录用户前置行为轨迹（<code>Breadcrumbs</code>）</p><p><strong>复现环境：</strong> 尽量还原机型、<code>OS</code> 版本、网络状态。利用移动端调试工具（<code>vConsole</code>, <code>Eruda</code>, <code>Chrome</code> <code>Inspect</code>）</p><p><strong>控制变量与灰度：</strong> 采用二分法隔离代码，降级回滚。是否某种特定数据格式或临界条件引发</p><p><strong>异常监控兜底：</strong> <code>window.onerror</code> &#x2F; 资源加载失败 &#x2F; 白屏监控（<code>MutationObserver</code> 检 <code>DOM</code> 树）</p><h3 id="调研市场上现有的跨端框架，讲明方法步骤"><a href="#调研市场上现有的跨端框架，讲明方法步骤" class="headerlink" title="调研市场上现有的跨端框架，讲明方法步骤"></a>调研市场上现有的跨端框架，讲明方法步骤</h3><ol><li>明确核心诉求 (包体积、性能、生态、团队现有技术栈 <code>Vue</code>&#x2F;<code>React</code>)</li><li>市场盘点 (收集主流方案：<code>Flutter</code>, <code>React Native</code>, <code>UniApp</code>, <code>Taro</code>, <code>Capacitor</code>)</li><li>构建评估矩阵</li></ol><ul><li><strong>性能表现：</strong> 渲染机制（<code>WebView</code> vs 原生控件 vs 自绘引擎）</li><li><strong>开发体验：</strong> 调试热更新、学习曲线</li><li><strong>生态与基建：</strong> 社区活跃度、第三方库丰富度</li></ul><ol start="4"><li><code>Demo</code> <code>PoC</code> 验证（做个核心业务的最小可行性产品，测试踩坑）</li><li>输出调研报告与选型建议</li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第9期 · 腾讯CSIG腾讯云一面</title>
      <link href="/2026/04/16/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F9/"/>
      <url>/2026/04/16/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F9/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>腾讯 CSIG 腾讯云 一面 4.16 19:00-19:40</strong></p></blockquote><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><h3 id="CA-证书"><a href="#CA-证书" class="headerlink" title="CA 证书"></a><code>CA</code> 证书</h3><p><strong>核心问题：怎么证明你访问的网站不是中间人伪造的？</strong></p><ul><li>根证书（<code>Root CA</code>，预装在你操作系统&#x2F;浏览器里）</li><li>中间证书（<code>Intermediate CA</code>）</li><li>网站证书（比如 <code>*.baidu.com</code>）</li></ul><ol><li>访问 <code>https://baidu.com</code>，服务器把自己的证书发给你</li><li>证书里包含：域名、公钥、有效期、签发者（<code>CA</code>）、<code>CA</code> 的数字签名</li><li>浏览器用预装的根 <code>CA</code> 公钥，逐级验签：根 <code>CA</code> 签中间 <code>CA</code> → 中间 <code>CA</code> 签网站证书</li><li>验签通过 &#x3D; 证书可信 &#x3D; 这个公钥确实属于 <code>baidu.com</code></li><li>然后用这个公钥协商出对称加密密钥（<code>TLS</code> 握手），后续通信全部加密</li></ol><p><strong>关键点：</strong></p><ul><li><code>CA</code> 证书本质是”用非对称加密做身份背书”</li><li>根 <code>CA</code> 是信任锚点，出厂就内置在系统里</li><li>如果根 <code>CA</code> 被攻破或滥发证书，整个信任链就崩了（历史上真发生过）</li></ul><h3 id="HTTP-2-多路复用"><a href="#HTTP-2-多路复用" class="headerlink" title="HTTP/2 多路复用"></a><code>HTTP/2</code> 多路复用</h3><p><strong>核心问题：<code>HTTP/1.1</code> 的致命缺陷叫队头阻塞（Head-of-Line Blocking）</strong></p><p><code>HTTP/2</code>：一个 <code>TCP</code> 连接上，同时跑多个请求&#x2F;响应</p><ul><li>帧（<code>Frame</code>）是最小单位</li><li>流（<code>Stream</code>）&#x3D; 一组帧，对应一个请求-响应对</li><li>同一个连接上：[Stream1-帧] [Stream3-帧] [Stream1-帧] [Stream2-帧] …</li></ul><p><code>HTTP/2</code> 把数据拆成二进制帧（<code>Frame</code>），每个帧带 <code>Stream ID</code></p><p>多个 <code>Stream</code> 的帧可以在同一个 <code>TCP</code> 连接上交替发送</p><p>接收端根据 <code>Stream ID</code> 重新组装</p><p>这样一个连接就能并行处理几十个请求，不用排队</p><p><strong>其他优势：</strong></p><p>头部压缩（<code>HPACK</code>）：<code>HTTP</code> 头很多是重复的（<code>Cookie</code>、<code>User-Agent</code>），压缩后节省带宽</p><p>服务器推送（<code>Server Push</code>）：服务器可以主动推 <code>CSS</code> &#x2F; <code>JS</code>，不用等浏览器请求</p><p><strong>注意：</strong> <code>HTTP/2</code> 没有解决 <code>TCP</code> 层的队头阻塞——如果 <code>TCP</code> 丢了一个包，所有 <code>Stream</code> 都得等重传。这就是 <code>HTTP/3</code> 换用 <code>QUIC</code>（基于 <code>UDP</code>）的原因</p><h3 id="Rules、MCP、Skills-的区别"><a href="#Rules、MCP、Skills-的区别" class="headerlink" title="Rules、MCP、Skills 的区别"></a><code>Rules</code>、<code>MCP</code>、<code>Skills</code> 的区别</h3><p>记住 <code>Rules</code> 是每次加载进上下文的，简单且好用</p><p>其他 略</p><h3 id="存储-API-之间的区别；缓存位置和存储-API-的区别"><a href="#存储-API-之间的区别；缓存位置和存储-API-的区别" class="headerlink" title="存储 API 之间的区别；缓存位置和存储 API 的区别"></a>存储 <code>API</code> 之间的区别；缓存位置和存储 <code>API</code> 的区别</h3><p><strong>！！常考常错，不要弄混缓存策略，缓存位置和存储<code>API</code>三者</strong></p><p>浏览器 &#x3D; 一个办公室</p><p>缓存位置 &#x3D; 快递收发室的货架</p><p>→ 上次收到的快递（<code>JS</code>&#x2F;<code>CSS</code>&#x2F;图片）先放这里</p><p>→ 下次要用，先看货架上有没有，有就不用再寄一次</p><p>→ <code>Memory Cache</code> &#x3D; 桌上（快但关灯就没了）</p><p>→ <code>Disk Cache</code> &#x3D; 柜子里（慢但持久）</p><p>→ <code>Service Worker Cache</code> &#x3D; 你自己管的专属储物柜</p><p>→ <code>Push Cache</code> &#x3D; 临时放一下的过道</p><p>存储<code>API</code> &#x3D; 办公桌的抽屉</p><p>→ 你主动往里放东西（用户登录态、偏好设置、离线数据）</p><p>→ <code>Cookie</code> &#x3D; 便签纸（小，每次出门都带着给别人看）</p><p>→ <code>LocalStorage</code> &#x3D; 小抽屉（5MB，一直在）</p><p>→ <code>SessionStorage</code> &#x3D; 临时便签（关门就扔）</p><p>→ <code>IndexedDB</code> &#x3D; 大文件柜（几百MB，结构化数据）</p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-13.png" alt="面经图 13"><br><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-14.png" alt="面经图 14"></p>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第8期 · 小红书一面</title>
      <link href="/2026/04/09/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F8/"/>
      <url>/2026/04/09/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F8/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>小红书 一面 4.9 11:00-11:40</strong></p></blockquote><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><h3 id="Vue3-的双向绑定（Proxy）底层怎么实现的"><a href="#Vue3-的双向绑定（Proxy）底层怎么实现的" class="headerlink" title="Vue3 的双向绑定（Proxy）底层怎么实现的"></a><code>Vue3</code> 的双向绑定（<code>Proxy</code>）底层怎么实现的</h3><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>对比维度</strong></td><td><strong>Vue 2</strong></td><td><strong>Vue 3 (Proxy)</strong></td></tr><tr><td><strong>代理粒度</strong></td><td>属性级，需递归遍历</td><td>对象级，一次代理</td></tr><tr><td><strong>新增&#x2F;删除属性</strong></td><td>无法监听需 <code>$set</code> &#x2F; <code>$delete</code></td><td>天然支持</td></tr><tr><td><strong>数组索引&#x2F;长度修改</strong></td><td>无法直接监听</td><td>完全支持</td></tr><tr><td><strong>Map&#x2F;Set 数据结构</strong></td><td>不支持</td><td>原生支持</td></tr><tr><td><strong>初始化性能</strong></td><td>递归劫持深层对象，慢</td><td>懒响应，仅访问时转换，快</td></tr></tbody></table><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-09.png" alt="面经图 9"></p><h3 id="同源策略"><a href="#同源策略" class="headerlink" title="同源策略"></a>同源策略</h3><p>同源策略是什么，为什么需要跨域 略</p><h3 id="CSRF"><a href="#CSRF" class="headerlink" title="CSRF"></a><code>CSRF</code></h3><p><code>CSRF</code> 种类，实际应用 略</p><h3 id="项目中一般用哪几个生命周期钩子；从后端调数据用哪个"><a href="#项目中一般用哪几个生命周期钩子；从后端调数据用哪个" class="headerlink" title="项目中一般用哪几个生命周期钩子；从后端调数据用哪个"></a>项目中一般用哪几个生命周期钩子；从后端调数据用哪个</h3><p>项目中一般用哪几个生命周期钩子比较多；如果要从后端调数据，应该用哪个钩子</p><p><strong>首选 <code>created</code>（<code>Vue</code> 2）</strong> 或直接在 <strong><code>setup</code> 顶层（<code>Vue</code> 3）</strong> 发起请求。</p><p>原因：数据请求与 <code>DOM</code> 渲染无关，在 <code>created</code> 阶段发起请求能更早拿到数据，缩短白屏时间。<strong>特殊情况用</strong> <code>mounted</code>：需要依赖 <code>DOM</code> 元素存在才能发起的请求（例如根据容器 <code>ID</code> 请求特定样式配置），或需要等待子组件挂载完毕的场景</p><h3 id="Skill-和-agent-的关系（包含-）"><a href="#Skill-和-agent-的关系（包含-）" class="headerlink" title="Skill 和 agent 的关系（包含?）"></a><code>Skill</code> 和 <code>agent</code> 的关系（包含?）</h3><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>概念</strong></td><td><strong>定义</strong></td><td><strong>类比</strong></td></tr><tr><td><strong>Agent</strong></td><td>自主决策单元，拥有记忆、规划、工具调用能力<br><br>负责理解用户意图、分解任务、选择工具&#x2F;技能并执行</td><td><strong>大脑+双手</strong></td></tr><tr><td><strong>Skill</strong></td><td>封装好的单一功能模块（如计算器、联网搜索、发送邮件、代码解释器）。提供明确的输入输出接口</td><td><strong>单个工具</strong></td></tr></tbody></table><h3 id="写代码的时候怎么处理幻觉"><a href="#写代码的时候怎么处理幻觉" class="headerlink" title="写代码的时候怎么处理幻觉"></a>写代码的时候怎么处理幻觉</h3><p><strong>编码阶段（<code>Prompt</code> Engineering）</strong></p><p><strong>严格限定上下文范围：</strong> 明确约束词</p><p><strong>强制结构化输出：</strong> 要求输出 <code>JSON</code> 格式 或 特定 <code>Schema</code></p><p><strong>分步推理：</strong> 使用 <code>Chain-of-Thought</code> 提示词，先分析需求再给出代码，降低跳跃性错误</p><p><strong>工程架构防御</strong></p><p><strong>RAG（检索增强生成）：</strong> 将企业私有代码库、最新文档向量化，检索后作为上下文喂给模型</p><p><strong>输出校验：</strong> 静态语法检查（<code>AST</code> 解析器检查语法）、沙箱执行验证（<code>Docker</code> 中运行代码）</p><p><strong>工具调用约束：</strong> 定义好函数签名，模型不负责生成执行逻辑，从根本上杜绝逻辑幻觉</p><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td>概念</td><td>抽象层次</td><td>职责</td></tr><tr><td><strong>函数签名</strong></td><td>接口规范</td><td>定义函数“长什么样”（名称、参数、类型）</td></tr><tr><td><strong>Function Calling</strong></td><td>模型能力</td><td>让模型能根据签名生成调用意图</td></tr><tr><td><strong>MCP</strong></td><td>通信协议</td><td>标准化模型与函数（工具）之间的请求&#x2F;响应流程</td></tr></tbody></table><p><strong>Human-in-the-Loop：</strong> 对关键业务代码（数据库操作、支付逻辑）设置人工确认卡点</p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-10.png" alt="面经图 10"><br><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-11.png" alt="面经图 11"><br><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-12.png" alt="面经图 12"></p><h2 id="手撕"><a href="#手撕" class="headerlink" title="手撕"></a>手撕</h2><p><strong>手写防抖节流</strong></p>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第7期 · 字节财经支付三面</title>
      <link href="/2026/04/02/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F7/"/>
      <url>/2026/04/02/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F7/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>字节 财经支付 三面 4.2 18:00-18:30</strong></p></blockquote><h2 id="项目"><a href="#项目" class="headerlink" title="项目"></a>项目</h2><p><strong>询问项目，讲整个项目的缘起和目的，聊到重金属、水体科研</strong></p><ul><li>介绍项目，从意义，价值，过程等方面展开讲讲</li><li>项目中的难点如何克服</li><li>未来的职业发展路径规划</li></ul><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><p><strong>八股：</strong></p><h3 id="HTTP-是什么"><a href="#HTTP-是什么" class="headerlink" title="HTTP 是什么"></a><code>HTTP</code> 是什么</h3><p><code>HyperText Transfer Protocol</code> 是一个<strong>应用层</strong>协议，用于在客户端和服务器之间传输数据</p><table><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>特性</strong></td><td><strong>说明</strong></td></tr><tr><td>无状态</td><td>每次请求独立</td></tr><tr><td>基于请求-响应</td><td>Client → Request → Server → Response</td></tr><tr><td>文本协议（HTTP&#x2F;1.1）</td><td>可读性强</td></tr><tr><td>可扩展</td><td>Header 可扩展</td></tr></tbody></table><h3 id="讲一讲浏览器输入-URL-并按下回车之后发生了什么"><a href="#讲一讲浏览器输入-URL-并按下回车之后发生了什么" class="headerlink" title="讲一讲浏览器输入 URL 并按下回车之后发生了什么"></a>讲一讲浏览器输入 <code>URL</code> 并按下回车之后发生了什么</h3><p><code>URL</code> 解析 → <code>DNS</code> 解析 → <code>TCP</code> 连接 → <code>HTTP</code> 请求 → 服务器处理 → 返回响应 → 渲染</p><h3 id="四次挥手可以由服务端发起嘛"><a href="#四次挥手可以由服务端发起嘛" class="headerlink" title="四次挥手可以由服务端发起嘛"></a>四次挥手可以由服务端发起嘛</h3><p>可以，<code>TCP</code> 是全双工，任何一方都可以主动关闭连接，谁先调用 <code>close()</code>，谁先发 <code>FIN</code></p><h3 id="TCP-与-UDP-的对比"><a href="#TCP-与-UDP-的对比" class="headerlink" title="TCP 与 UDP 的对比"></a><code>TCP</code> 与 <code>UDP</code> 的对比</h3><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>特性</strong></td><td><strong>TCP</strong></td><td><strong>UDP</strong></td></tr><tr><td>是否连接</td><td>有</td><td>无</td></tr><tr><td>是否可靠</td><td>是</td><td>否</td></tr><tr><td>是否有序</td><td>是</td><td>否</td></tr><tr><td>速度</td><td>慢</td><td>快</td></tr><tr><td>使用场景</td><td>HTTP、文件传输</td><td>视频、游戏、DNS</td></tr></tbody></table><h3 id="UDP-协议下，两端是怎么连接的"><a href="#UDP-协议下，两端是怎么连接的" class="headerlink" title="UDP 协议下，两端是怎么连接的"></a><code>UDP</code> 协议下，两端是怎么连接的</h3><p><code>UDP</code> 不建立连接，直接通过 <strong>IP</strong> 和<strong>端口</strong>发送数据</p>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第6期 · 字节财经支付二面</title>
      <link href="/2026/04/01/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F6/"/>
      <url>/2026/04/01/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F6/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>字节 财经支付 二面 4.1 18:00-19:00</strong></p></blockquote><h2 id="项目"><a href="#项目" class="headerlink" title="项目"></a>项目</h2><p><strong>打开项目页面展示，不断追问具体实现</strong></p><ul><li>斑块怎么处理的 <strong><code>Geoserver</code></strong></li><li>有没有对图像做优化 <strong>降分辨率</strong></li></ul><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><p><strong>八股：</strong></p><h3 id="GET-POST-的区别"><a href="#GET-POST-的区别" class="headerlink" title="GET &#x2F; POST 的区别"></a><code>GET</code> &#x2F; <code>POST</code> 的区别</h3><p><code>GET</code>：用于<strong>从服务器获取资源</strong>。比如浏览网页、下载图片、查询数据等。<code>GET</code> 请求通常只读取数据，不会改变服务器的状态（幂等、安全）。参数一般附在 <code>URL</code> 后面，有长度限制，不适宜传递敏感信息。</p><p><code>POST</code>：用于<strong>向服务器提交数据</strong>，比如提交表单、上传文件、发表评论等。<code>POST</code> 请求不一定是更新已有资源，它更常见的语义是<strong>创建新资源或触发服务器处理逻辑</strong></p><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>对比点</strong></td><td><strong>GET</strong></td><td><strong>POST</strong></td></tr><tr><td>用途</td><td>获取数据</td><td>提交数据</td></tr><tr><td>参数位置</td><td>URL</td><td>请求体</td></tr><tr><td>是否缓存</td><td>会</td><td>一般不会</td></tr><tr><td>安全性</td><td>低</td><td>相对高</td></tr><tr><td>幂等性</td><td>幂等</td><td>非幂等</td></tr><tr><td>长度限制</td><td>有</td><td>基本无</td></tr><tr><td>可见性</td><td>明文 URL</td><td>不可见</td></tr></tbody></table><h3 id="状态码"><a href="#状态码" class="headerlink" title="状态码"></a>状态码</h3><p>2xx，3xx，4xx，5xx状态码 略</p><h2 id="大模型"><a href="#大模型" class="headerlink" title="大模型"></a>大模型</h2><p><strong>开始大模型考察</strong></p><h3 id="Skills-和-MCP-的关系"><a href="#Skills-和-MCP-的关系" class="headerlink" title="Skills 和 MCP 的关系"></a><code>Skills</code> 和 <code>MCP</code> 的关系</h3><p>略</p><h3 id="Cursor-好用的点是什么"><a href="#Cursor-好用的点是什么" class="headerlink" title="Cursor 好用的点是什么"></a><code>Cursor</code> 好用的点是什么</h3><p><code>Cursor</code> 是一款基于 <code>VS Code</code> 重构的 <code>AI IDE</code>，不是单纯的聊天插件，而是把 <code>AI</code> 当成程序员，从代码补全、重构、调试到项目级问答都更自然流畅</p><table><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>特性</strong></td><td><strong>说明</strong></td></tr><tr><td>AI 深度集成</td><td>代码编辑器内直接调用 LLM</td></tr><tr><td>上下文理解强</td><td>能理解整个项目</td></tr><tr><td>自动补全</td><td>比传统 IDE 更智能</td></tr><tr><td>Agent能力</td><td>可修改多文件</td></tr><tr><td>Chat + Code融合</td><td>边聊边改代码</td></tr></tbody></table><h3 id="文本的-token-是什么，多模态的-token-是什么"><a href="#文本的-token-是什么，多模态的-token-是什么" class="headerlink" title="文本的 token 是什么，多模态的 token 是什么"></a>文本的 <code>token</code> 是什么，多模态的 <code>token</code> 是什么</h3><p><code>token</code> 是模型处理对象的最小单位（不是单词）</p><table><thead><tr><th></th><th></th></tr></thead><tbody><tr><td><strong>类型</strong></td><td><strong>token 表示</strong></td></tr><tr><td>文本</td><td>字&#x2F;词</td></tr><tr><td>图片</td><td>patch（图像块）</td></tr><tr><td>音频</td><td>时间片段</td></tr><tr><td>视频</td><td>帧序列</td></tr></tbody></table><h3 id="Encoder-和-Decoder-的作用和区别"><a href="#Encoder-和-Decoder-的作用和区别" class="headerlink" title="Encoder 和 Decoder 的作用和区别"></a><code>Encoder</code> 和 <code>Decoder</code> 的作用和区别</h3><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>对比</strong></td><td><strong>Encoder</strong></td><td><strong>Decoder</strong></td></tr><tr><td>作用</td><td>理解输入</td><td>生成输出</td></tr><tr><td>输入</td><td>原始数据</td><td>编码后的表示</td></tr><tr><td>输出</td><td>向量表示</td><td>预测结果</td></tr><tr><td>典型模型</td><td>BERT</td><td>GPT</td></tr><tr><td>是否生成</td><td>否</td><td>是</td></tr></tbody></table><h3 id="大语言模型怎么实现的"><a href="#大语言模型怎么实现的" class="headerlink" title="大语言模型怎么实现的"></a>大语言模型怎么实现的</h3><p>大语言模型本质是：用 <code>Transformer</code> 结构训练一个【预测下一个 <code>token</code> 的概率模型】</p><p>数据 → 分词 → 向量化 → <code>Transformer</code> → 训练 → 微调 → 推理</p><h3 id="RAG-怎么具体实现"><a href="#RAG-怎么具体实现" class="headerlink" title="RAG 怎么具体实现"></a><code>RAG</code> 怎么具体实现</h3><p><strong>索引构建：</strong></p><ol><li><p>加载与切分</p></li><li><p>向量化</p></li><li><p>存储进向量数据库</p></li></ol><p><strong>检索生成：</strong></p><ol start="4"><li><p>检索相似文本块</p></li><li><p>增强提示词</p></li><li><p>生成</p></li></ol><p><strong>核心步骤：</strong></p><ul><li><strong>文本切分 (Chunking)：</strong> 不是简单粗暴地切。好的切分策略会考虑语义完整性，比如按段落、按标题切，并让块与块之间有点小重叠（例如500字一块，重叠50字），防止把一个完整的信息切断。</li><li><strong>向量化 (Embedding)：</strong> 选择哪个“嵌入模型”很关键。有通用的（如 <code>OpenAI</code> 的 <code>text-embedding-3-small</code>），也有专门针对中文优化的（如 <code>BAAI/bge-large-zh</code>），选对了能显著提升检索质量。</li><li><strong>向量数据库：</strong> 选哪个库取决于你的场景。本地开发或原型验证可以用轻量级的 <code>Chroma</code> 或 <code>FAISS</code>；企业级大规模应用则可以考虑 <code>Milvus</code> 或云端的 <code>Pinecone</code>。</li><li><strong>Prompt设计：</strong> 这是“生成”环节的灵魂。你需要清晰地告诉模型：“请严格基于以下参考资料回答，如果资料里没有，就说不知道”。这能有效约束模型，让它不乱说。</li></ul><p><strong>进阶优化：</strong></p><ul><li><strong>混合检索：</strong> 将“向量搜索”（语义）和“关键词搜索”（如 <code>BM25</code> 算法，精确匹配）结合，能同时理解意思和抓住专有名词，效果1+1&gt;2。</li><li><strong>重排序 (Rerank)：</strong> 检索出 <code>Top-K</code> 个结果后，用一个更精密的模型（如 <code>NVIDIA</code> <code>NeMo Reranker</code>）对它们重新打分排序，把最相关的信息排在前面，能极大提升最后生成答案的质量。</li><li><strong>查询改写：</strong> 用户的问题可能很口语化或信息不全。用大模型先对原始问题进行润色、扩写，生成几个更清晰、更适合检索的问题再去查资料，召回率会更高。</li></ul><h2 id="代码题"><a href="#代码题" class="headerlink" title="代码题"></a>代码题</h2><p><strong>代码题：字符串转数字</strong></p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-08.png" alt="面经图 8"></p>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第5期 · 腾讯IEG品牌团队UI一面</title>
      <link href="/2026/04/01/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F5/"/>
      <url>/2026/04/01/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F5/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>腾讯 IEG品牌团队 UI开发 一面 4.1 16:00-17:20</strong></p></blockquote><h2 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h2><ul><li><strong>回答问题不要太发散，说了太多废话，应该专注于问题核心</strong></li></ul><h2 id="项目与八股"><a href="#项目与八股" class="headerlink" title="项目与八股"></a>项目与八股</h2><h3 id="为什么选-Element-UI"><a href="#为什么选-Element-UI" class="headerlink" title="为什么选 Element UI"></a>为什么选 <code>Element UI</code></h3><ul><li><p>基础框架组件：<code>el-menu</code> &#x2F; <code>el-menu-item</code>（顶部导航切模块），<code>el-avatar</code>（用户区）</p></li><li><p>登录表单与校验：<code>el-form</code> &#x2F; <code>el-form-item</code> &#x2F; <code>el-input</code>（账号密码+校验），<code>v-loading</code>（登录加载态）</p></li><li><p>全局交互反馈：<code>this.$message</code>（成功&#x2F;失败提示），<code>this.$confirm</code>（退出确认弹窗）</p></li><li><p>业务面板里的常用组件：<code>el-collapse</code>（图层&#x2F;专题折叠面板），<code>el-slider</code>（时间滑块），<code>el-checkbox</code>（开关类），<code>el-tooltip</code>&#x2F;<code>tag</code>&#x2F;<code>empty</code>（提示、标签、空态）</p></li><li><p>主要用它做表单、导航、弹窗提示和一些面板交互，<code>Element UI</code> 在 <code>Vue2</code> 生态里成熟稳定、资料多、上手快，能把通用交互快速落地，把主要精力放在 <code>Mapbox</code>+<code>GeoServer</code> 的 <code>GIS</code> 业务实现上，<code>Ant Design Vue</code> 也很好，但当时切换成本更高、收益不明显</p></li></ul><h3 id="父子组件的生命周期"><a href="#父子组件的生命周期" class="headerlink" title="父子组件的生命周期"></a>父子组件的生命周期</h3><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-07.png" alt="面经图 7"></p><h3 id="git-merge-和-git-rebase-的区别"><a href="#git-merge-和-git-rebase-的区别" class="headerlink" title="git merge 和 git rebase 的区别"></a><code>git merge</code> 和 <code>git rebase</code> 的区别</h3><table><thead><tr><th>对比点</th><th><code>merge</code></th><th><code>rebase</code></th></tr></thead><tbody><tr><td>本质</td><td>合并分支</td><td>变基（移动提交）</td></tr><tr><td>历史记录</td><td>保留分叉</td><td>线性历史</td></tr><tr><td>是否产生新 <code>commit</code></td><td>会（<code>merge commit</code>）</td><td>不一定</td></tr><tr><td>可读性</td><td>一般</td><td>更清晰</td></tr><tr><td>冲突处理</td><td>一次性</td><td>可能多次</td></tr><tr><td>是否改写历史</td><td>否</td><td>是（危险）</td></tr><tr><td>适用场景</td><td>团队协作</td><td>个人分支整理</td></tr></tbody></table><h3 id="Webpack-和-Vite-中的-HMR-热更新"><a href="#Webpack-和-Vite-中的-HMR-热更新" class="headerlink" title="Webpack 和 Vite 中的 HMR 热更新"></a><code>Webpack</code> 和 <code>Vite</code> 中的 <code>HMR</code> 热更新</h3><table><thead><tr><th>对比点</th><th><code>Webpack HMR</code></th><th><code>Vite HMR</code></th></tr></thead><tbody><tr><td>启动方式</td><td>打包后启动</td><td>原生 <code>ESM</code></td></tr><tr><td>构建速度</td><td>慢</td><td>极快</td></tr><tr><td>更新机制</td><td>整体重新打包模块</td><td>按需更新模块</td></tr><tr><td>底层原理</td><td><code>bundle</code> + <code>websocket</code></td><td>浏览器 <code>ESM</code> + <code>websocket</code></td></tr><tr><td>热更新粒度</td><td>较粗</td><td>非常细</td></tr><tr><td>开发体验</td><td>一般</td><td>很流畅</td></tr></tbody></table><ul><li><code>Webpack</code>：先打包再更新</li><li><code>Vite</code>：按需加载，直接替换模块</li><li><code>HMR</code> &#x3D; 文件变更 → 重新编译 → 通知浏览器 → 拉取新模块 → 局部替换</li><li><ol><li>监听文件变化</li></ol></li><li><ol start="2"><li>重新编译（内存中）</li></ol></li><li><ol start="3"><li>通知浏览器（<code>WebSocket</code>）<strong><code>WebSocket</code></strong> 是一种基于 <strong><code>TCP</code></strong> 的网络协议，专为实现<strong>实时双向通信</strong>而设计。它允许客户端和服务器之间建立一个持久连接，从而实现数据的高效传输</li></ol></li><li><ol start="4"><li>浏览器获取更新信息（<code>hash</code>）</li></ol></li><li><ol start="5"><li>拉取更新模块（<code>json</code> + <code>js</code>）</li></ol></li><li><ol start="6"><li>模块热替换（不刷新页面）</li></ol></li></ul><h2 id="场景题"><a href="#场景题" class="headerlink" title="场景题"></a>场景题</h2><ul><li>开发一套智能课程管理 <code>agent</code>，从前后端到模型选择</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第4期 · 字节财经支付一面</title>
      <link href="/2026/03/30/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F4/"/>
      <url>/2026/03/30/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F4/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>字节 财经支付 一面 3.30 15:00</strong></p></blockquote><h2 id="手撕"><a href="#手撕" class="headerlink" title="手撕"></a>手撕</h2><p>2 道 <code>coding</code></p><h3 id="1-字符串-DIFF-页面"><a href="#1-字符串-DIFF-页面" class="headerlink" title="1. 字符串 DIFF 页面"></a>1. 字符串 <code>DIFF</code> 页面</h3><p>（<code>vibe coding</code>）实现一个能够考虑空白字符、零宽字符、换行符等特殊字符的字符串 <code>DIFF</code> 交互页面，基本要求如下：​</p><ul><li>不依赖任何前端框架与工程脚手架，最终产物为内联了 <code>CSS</code> 与 <code>JS</code> 的 <code>html</code> 文件；​</li><li>界面有两个输入框，第一个字符串代表原始字符串，第二个字符串代表变更后的字符串；​</li><li>界面有一个 <code>DIFF</code> 点击按钮，点击以后展示字符串对比效果；​</li><li>字符串对比界面交互与颜色参考 <code>github</code>，只需要考虑左右对比模式；​</li><li>字符串 <code>DIFF</code> 需考虑空白字符、零宽字符、换行符等特殊字符的友好展示；​</li></ul><h3 id="2-基于-Vue-的倒计时"><a href="#2-基于-Vue-的倒计时" class="headerlink" title="2. 基于 Vue 的倒计时"></a>2. 基于 <code>Vue</code> 的倒计时</h3><p>做一个基于 <code>vue</code> 的倒计时组件，需要接受入参 <code>time</code>，单位为秒，代表要倒计时的秒数​，并转换成 <code>时:分:秒</code> 展示倒计时，每隔 1 秒更新倒计时。​<del>设计三个控制按钮，一个 开始，一个 暂停，一个重置。</del>（可能会存在计时不准确的问题，怎么解决）</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">&amp;lt;</span>script<span class="symbol">&amp;gt;</span></span><br><span class="line">export default &#123;</span><br><span class="line">  data() &#123;</span><br><span class="line">    return &#123;</span><br><span class="line">      target: new Date(&#x27;2026-12-31 23:59:59&#x27;).getTime(),</span><br><span class="line">      now: Date.now(),</span><br><span class="line">      timer: null</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  computed: &#123;</span><br><span class="line">    isEnd() &#123;</span><br><span class="line">      return this.target - this.now <span class="symbol">&amp;lt;</span>= 0</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  mounted() &#123;</span><br><span class="line">    this.timer = setInterval(() =&gt; &#123;</span><br><span class="line">      this.now = Date.now()</span><br><span class="line">    &#125;, 1000)</span><br><span class="line">  &#125;,</span><br><span class="line">  beforeDestroy() &#123;</span><br><span class="line">    clearInterval(this.timer)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="symbol">&amp;lt;</span>/script<span class="symbol">&amp;gt;</span></span><br></pre></td></tr></table></figure><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-05.png" alt="面经图 5"><br><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-06.png" alt="面经图 6"></p><ul><li>倒计时手写不出来的时候基本上已经结束了，还是又问了我几个关于 <code>agent</code> 的问题</li></ul><h2 id="问答题"><a href="#问答题" class="headerlink" title="问答题"></a>问答题</h2><ul><li><ol start="0"><li>学习前端的路径，学习 <code>ai</code> 的路径 略</li></ol></li><li><ol><li><code>mcp</code> 和 <code>skill</code> 的区别 渐进式披露 消耗 <code>token</code> 量 <code>MCP</code> 的两种调用方式</li></ol></li><li><ol start="2"><li>平常工作用什么 <code>skill</code> 比较多，有常用的吗</li></ol></li><li><strong>3.</strong> 最近接触什么 <code>agent</code> 相关的新名词？<strong>Harness Engineering</strong></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第3期 · 京东一面</title>
      <link href="/2026/03/30/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F3/"/>
      <url>/2026/03/30/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F3/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>京东 一面 3.30 14:00-14:30</strong></p></blockquote><h2 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h2><p>两位面试官（<code>hr</code>+技术）<code>hr</code> 先问自我介绍+到岗时长+地点等问题，然后开始技术面，反复拷打项目，问了 9-10 道关于项目的问题，基本上没答出来几道，最后 <code>hr</code> 总结，反问环节表示项目并不是我从 0-1 搭建的，理解的还不够深刻</p><h2 id="单例、组件化、地图"><a href="#单例、组件化、地图" class="headerlink" title="单例、组件化、地图"></a>单例、组件化、地图</h2><h3 id="单例化和组件化了什么-Tmap"><a href="#单例化和组件化了什么-Tmap" class="headerlink" title="单例化和组件化了什么 Tmap"></a>单例化和组件化了什么 <code>Tmap</code></h3><ul><li><code>Mapbox map</code> 只创建一次，多个 <code>Vue</code> 控件&#x2F;图层组件通过 <code>TMap.getInstance()</code> 共享同一张地图；组件复用体现在控件和图层封装成独立 <code>.vue</code> 组件，可插拔组合使用，而 <code>TMap</code> 是提供共享 <code>map</code> 的服务模块，不是 <code>Vue</code> 组件本身</li><li>单例化：一种「对象管理策略」，核心是「全局只保留一个实例」</li><li>组件化：一种「代码组织方式」，核心是「按职责拆模块，复用与维护更好」</li></ul><h3 id="3D-视角切换"><a href="#3D-视角切换" class="headerlink" title="3D 视角切换"></a>3D 视角切换</h3><ul><li>3D 视角切换是通过 <code>UI</code> 按钮触发，底层调用 <code>Mapbox</code> <code>flyTo</code> 修改相机参数（<code>pitch</code>、<code>bearing</code>），依托 <code>TMap</code> 单例保证所有控件都操作同一张地图</li></ul><h3 id="图层级交互"><a href="#图层级交互" class="headerlink" title="图层级交互"></a>图层级交互</h3><ul><li><p>完成图层级交互能力建设（<code>hover</code> 高亮、<code>click</code> 定位、<code>flyTo</code>&#x2F;<code>fitBounds</code>、图层可见性&#x2F;透明度&#x2F;顺序动态调整），保障复杂地图场景下的可用性与可维护性，这一句怎么实现的</p></li><li><p>已修改为「完成图层级交互能力建设，支持要素悬停高亮、点击定位（<code>flyTo</code>&#x2F;<code>fitBounds</code>）、图层可见性切换、透明度调整与图层顺序重排，保证多图层场景下的可用性与可维护性」</p></li><li><p>登录验证码怎么实现的，有跟后端结合吗 略</p></li></ul><h3 id="MapboxGL-ECharts-是什么"><a href="#MapboxGL-ECharts-是什么" class="headerlink" title="MapboxGL &#x2F; ECharts 是什么"></a><code>MapboxGL</code> &#x2F; <code>ECharts</code> 是什么</h3><ul><li><strong>MapboxGL：</strong> 浏览器端的 <code>WebGL</code> 地图库，用来渲染可交互地图，并支持矢量&#x2F;栅格&#x2F;热力&#x2F;地形、图层样式表达式以及事件交互；你项目用它做 <code>GIS</code> 可视化底座 + 图层&#x2F;控件体系</li><li><strong>ECharts：</strong> 百度开源的前端图表库，用 <code>option</code> 配置渲染柱状图&#x2F;饼图等，项目里用于统计可视化</li></ul><h3 id="防抖、节流"><a href="#防抖、节流" class="headerlink" title="防抖、节流"></a>防抖、节流</h3><ul><li>防抖（<code>Debounce</code>）在事件停止触发后执行一次函数【延迟执行，避免重复触发】</li><li>节流（<code>Throttle</code>）在事件持续触发时按固定时间间隔执行函数【限制函数执行频率】</li></ul><h2 id="异步竞态"><a href="#异步竞态" class="headerlink" title="异步竞态"></a>异步竞态</h2><ul><li>页面上有两个按键，按下 <code>a</code> 由于网络问题没有反应，然后按下 <code>b</code>，<code>b</code> 的数据成功加载了，之后这个页面的数据会怎么变，<code>a</code> 对应的业务逻辑还会触发嘛，数据还会显示嘛</li></ul><p>考察你对<strong>异步请求的竞态条件（<code>Race Condition</code>）</strong>、<strong>用户交互的响应设计</strong>以及<strong>网络异常处理：</strong></p><ul><li>这个问题涉及前端异步请求的<strong>竞态条件</strong>处理。如果代码没有做特殊控制，那么 <code>A</code> 的请求虽然延迟了，但最终返回时依然会触发它的回调，导致页面数据被 <code>A</code> 覆盖，用户明明最后点了 <code>B</code>，看到的却是 <code>A</code> 的数据，这是一个典型的 <code>Bug</code>。</li><li>在实际开发中，我们需要处理这种情况。比较常见的方案是使用 <code>AbortController</code> 在发起 <code>B</code> 请求时取消 <code>A</code> 请求，或者利用请求 <code>ID</code>（<code>requestID</code>）来忽略过时响应。这样做的结果是：<code>A</code> 的业务逻辑要么被取消，要么响应被丢弃，页面最终只展示 <code>B</code> 的数据。</li><li>此外，针对「按下 <code>A</code> 没有反应」这个描述，我也认为这是体验问题。在用户点击 <code>A</code> 时，我们应该立即给用户一个视觉反馈（如按钮置灰、<code>loading</code> 状态），并设置超时机制，避免用户因「没反应」而误操作 <code>B</code>。本质上是考察我对异步竞态、用户体验和错误降级的综合处理能力。</li></ul><h2 id="CSS-的居中"><a href="#CSS-的居中" class="headerlink" title="CSS 的居中"></a><code>CSS</code> 的居中</h2><p>（<strong>各种居中方式都要掌握</strong>）</p><p><strong>水平：</strong></p><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>方法</strong></td><td><strong>适用场景</strong></td><td><strong>写法</strong></td></tr><tr><td><code>margin: 0 auto</code></td><td>块级元素 + 定宽</td><td><code>margin: 0 auto;</code></td></tr><tr><td><code>text-align</code></td><td>行内 &#x2F; <code>inline-block</code></td><td>父：<code>text-align: center</code></td></tr><tr><td><code>flex</code></td><td>通用</td><td><code>justify-content: center</code></td></tr><tr><td><code>grid</code></td><td>通用</td><td><code>justify-content: center</code></td></tr><tr><td><code>absolute</code> + <code>transform</code></td><td>任意元素</td><td><code>left:50%; transform:translateX(-50%)</code></td></tr></tbody></table><ul><li><code>margin: auto</code> 必须有宽度</li><li><code>text-align</code> 只对行内元素有效</li><li><code>transform</code> 不会影响文档流</li></ul><p><strong>垂直：</strong></p><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>方法</strong></td><td><strong>适用场景</strong></td><td><strong>写法</strong></td></tr><tr><td><code>flex</code></td><td>最常用</td><td><code>align-items: center</code></td></tr><tr><td><code>grid</code></td><td>简洁</td><td><code>place-items: center</code></td></tr><tr><td><code>line-height</code></td><td>单行文本</td><td><code>line-height = height</code></td></tr><tr><td><code>absolute</code> + <code>transform</code></td><td>通用</td><td><code>top:50%; transform:translateY(-50%)</code></td></tr><tr><td><code>table-cell</code></td><td>兼容旧浏览器</td><td><code>display: table-cell; vertical-align: middle</code></td></tr></tbody></table><ul><li><code>line-height</code>❗ 只适用于单行文本</li><li><code>vertical-align: middle</code>❗ 必须配合 <code>table-cell</code> 或 <code>inline</code></li><li><code>height: auto</code>❗ 不能用 <code>line-height</code> 居中</li><li><code>flex</code> 默认是横向（别写错）</li></ul><p><strong>水平+垂直：</strong></p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-03.png" alt="面经图 3"></p><ul><li><code>absolute</code> 必须有 定位父元素</li><li><code>transform</code> 是基于自身尺寸</li><li><code>flex</code> 会影响子元素布局（比如变成弹性项）</li></ul><p><strong>文本：</strong></p><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>类型</strong></td><td><strong>方法</strong></td><td><strong>写法</strong></td></tr><tr><td>水平</td><td><code>text-align</code></td><td><code>text-align: center</code></td></tr><tr><td>垂直（单行）</td><td><code>line-height</code></td><td>同高度</td></tr><tr><td>多行</td><td><code>flex</code></td><td><code>align-items: center</code></td></tr><tr><td>多行</td><td><code>table-cell</code></td><td><code>vertical-align: middle</code></td></tr></tbody></table><ul><li><p>多行文本❌ 不能用 <code>line-height</code></p></li><li><p><code>vertical-align</code>❌ 对 <code>block</code> 元素无效</p></li><li><p>「<code>CSS</code> 面试坑点大全（比如 <code>BFC</code>、层叠上下文、<code>z-index</code>）」</p></li></ul><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-04.png" alt="面经图 4"></p>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第2期 · 阿里千问C端一面</title>
      <link href="/2026/03/27/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F2/"/>
      <url>/2026/03/27/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F2/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>阿里 千问C端一面 3.27 16:00-16:45</strong></p></blockquote><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><p><strong>八股：</strong></p><h3 id="项目优化方式"><a href="#项目优化方式" class="headerlink" title="项目优化方式"></a>项目优化方式</h3><p><strong>单例地图实例：</strong> 全站只创建一次 <code>Mapbox Map</code>，控件&#x2F;图层组件都复用同一个 <code>map</code>，避免重复初始化与重复加载样式资源（<code>src/components/utils/tmap.js</code> 的 <code>Tmap.getInstance()</code>）</p><p><strong>防抖&#x2F;节流：</strong></p><p><strong>Webpack 打包方式：</strong></p><h3 id="登录与数据流"><a href="#登录与数据流" class="headerlink" title="登录与数据流"></a>登录与数据流</h3><p><strong>问：</strong> 登录系统怎么做的，登录验证码怎么实现的</p><ul><li>路由转发至后端，后端执行验证逻辑</li><li>纯前端图形验证码，随机生成 4 位数字用 <code>canvas</code> 画出，登录时比对</li></ul><p><strong>问：</strong> 数据从前端到后端是怎么流转的</p><ul><li><strong>前端发起：</strong> 组件事件触发 → 调 <code>service.js</code> 里封装的 API（<code>axios</code>）</li><li><strong>请求路径：</strong> 统一走 <code>/service/...</code></li><li><strong>开发代理：</strong> <code>config/index.js</code> 里 <code>proxyTable: { &#39;/service&#39;: { target: &#39;http://localhost:8087&#39; } }</code>，所以本地开发不会跨域</li><li><strong>后端返回：</strong> 前端接收返回值来更新 <code>UI</code>&#x2F;<code>状态</code></li></ul><h3 id="CSRF-Vue-和-React"><a href="#CSRF-Vue-和-React" class="headerlink" title="CSRF &#x2F; Vue 和 React"></a>CSRF &#x2F; <code>Vue</code> 和 <code>React</code></h3><ul><li>CSRF 种类 略</li><li><code>Vue</code> 和 <code>React</code> 的区别</li></ul><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>对比维度</strong></td><td><strong>Vue.js</strong></td><td><strong>React</strong></td></tr><tr><td>定位</td><td>渐进式框架</td><td><code>UI</code> 库</td></tr><tr><td>数据流</td><td>双向绑定</td><td>单向数据流</td></tr><tr><td>核心原理</td><td>响应式系统（依赖追踪）</td><td>函数式 + <code>Hooks</code> + 重新渲染</td></tr><tr><td>模板语法</td><td><code>Template</code></td><td><code>JSX</code></td></tr><tr><td>上手难度</td><td>低</td><td>中等</td></tr><tr><td>灵活性</td><td>较低（约定多）</td><td>高（自由度大）</td></tr></tbody></table><h3 id="Cookie-Session-的区别"><a href="#Cookie-Session-的区别" class="headerlink" title="Cookie &#x2F; Session 的区别"></a><code>Cookie</code> &#x2F; <code>Session</code> 的区别</h3><ul><li><code>Cookie</code> 存客户端，<code>Session</code> 存服务端，通过 <code>SessionID</code> 关联</li></ul><table><thead><tr><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>对比维度</strong></td><td><strong>Cookie</strong></td><td><strong>Session</strong></td></tr><tr><td>存储位置</td><td>浏览器</td><td>服务器</td></tr><tr><td>安全性</td><td>较低（可被篡改）</td><td>较高</td></tr><tr><td>存储大小</td><td>≈ 4KB</td><td>取决于服务器</td></tr><tr><td>生命周期</td><td>可设置过期时间</td><td>通常随会话结束</td></tr><tr><td>网络开销</td><td>每次请求都会携带</td><td>只传 <code>SessionID</code></td></tr><tr><td>依赖关系</td><td>存储 <code>SessionID</code></td><td>通过 <code>SessionID</code> 识别用户</td></tr></tbody></table><h2 id="学习"><a href="#学习" class="headerlink" title="学习"></a>学习</h2><ul><li>怎么学习前后端知识的 √</li><li>怎么学习 <code>AI</code> 的 √</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>第1期 · 腾讯CDG金融科技一面</title>
      <link href="/2026/03/27/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F1/"/>
      <url>/2026/03/27/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F1/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>腾讯CDG金融科技 一面 3.27 14:30-15:45</strong></p></blockquote><h2 id="八股"><a href="#八股" class="headerlink" title="八股"></a>八股</h2><p><strong>八股：</strong></p><h3 id="原型链"><a href="#原型链" class="headerlink" title="原型链"></a>原型链</h3><ul><li><strong>问：</strong> 原型链是什么 原型链存在的意义</li><li><strong>答：</strong> 原型链是对象通过 <code>__proto__</code> 向上查找属性形成的链式结构，是 JS 实现继承和属性共享（节约内存）的机制</li></ul><h3 id="Promise-链式调用"><a href="#Promise-链式调用" class="headerlink" title="Promise 链式调用"></a>Promise 链式调用</h3><ul><li><strong>问：</strong> Promise链式调用是怎么做到的？</li><li><strong>答：</strong> Promise 的链式调用是通过 <code>.then()</code> 方法实现的。每次调用 <code>.then()</code> 都会返回一个新的 Promise 对象，这个新 Promise 的状态取决于回调函数的返回值或抛出的错误。即使回调函数没有写 <code>return</code>，它也会隐式返回 <code>undefined</code>，从而让新 Promise 以 <code>undefined</code> 为结果继续传递。这样我们就能通过连续的 <code>.then()</code> 来处理异步操作的结果</li></ul><h3 id="CSRF"><a href="#CSRF" class="headerlink" title="CSRF"></a>CSRF</h3><p>CSRF：<strong>跨站伪造请求</strong>——本质是利用浏览器自动携带 <code>Cookie</code>，在用户不知情下发请求</p><table><thead><tr><th></th><th></th><th></th><th></th></tr></thead><tbody><tr><td><strong>类型</strong></td><td><strong>攻击方式</strong></td><td><strong>典型手段</strong></td><td><strong>特点</strong></td></tr><tr><td>GET 型</td><td>GET 请求发起</td><td>&lt;img&gt;、&lt;a&gt;</td><td>最简单，隐蔽性一般</td></tr><tr><td>POST 表单型</td><td>伪造表单提交</td><td>自动提交 &lt;form&gt;</td><td>更常见，可构造复杂请求</td></tr><tr><td>链接诱导型</td><td>诱导用户点击</td><td>钓鱼链接</td><td>依赖用户交互</td></tr></tbody></table><h3 id="同源策略"><a href="#同源策略" class="headerlink" title="同源策略"></a>同源策略</h3><p>同源策略：浏览器限制不同源之间的资源访问</p><p>同源的三个条件：协议&#x2F;域名&#x2F;端口全部相同</p><p>限制【<code>DOM</code> 访问 &#x2F; <code>Cookie</code> &#x2F; <code>LocalStorage</code> &#x2F; <code>AJAX</code>】请求以防止跨站攻击（如 <code>CSRF</code>、<code>XSS</code>）</p><h3 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h3><p>闭包：函数 + 其词法作用域的组合</p><p>核心特点：可以访问函数外部变量 &#x2F; 变量不会被销毁（被持续引用）</p><p>典型作用：数据私有化&#x2F;函数工厂&#x2F;防抖&#x2F;节流</p><h3 id="HTTP-和-HTTPS"><a href="#HTTP-和-HTTPS" class="headerlink" title="HTTP 和 HTTPS"></a>HTTP 和 HTTPS</h3><p><code>HTTP</code> 和 <code>HTTPS</code> 的区别 <strong>TLS传输层安全协议 &#x2F; SSL安全套接层 端口80 &#x2F; 443</strong></p><p><code>HTTPS</code> 在 <code>TCP</code> 三次握手之后，还需进行 <code>SSL/TLS</code>（提供数据加密、身份验证、完整性保护）的握手过程，才可进入加密报文传输</p><p>1.建立 <code>TCP</code> 连接。</p><p>2.进行 <code>SSL/TLS</code> 握手，协商加密算法和密钥。</p><p><code>TLS</code> 握手过程如下： ① <code>Client Hello</code>（客户端问候） ② <code>Server Hello</code>（服务器问候） ③ <code>Certificate</code>（证书） ④ <code>Certificate Verification</code>（证书验证） ⑤ <code>Key Exchange</code>（密钥交换） ⑥ <code>Session Key Generation</code>（会话密钥生成） ⑦ <code>Change Cipher Spec</code>（更改密码规范） ⑧ <code>Finished</code></p><p>3.客户端和服务器使用协商好的密钥进行加密通信</p><h3 id="函数柯里化"><a href="#函数柯里化" class="headerlink" title="函数柯里化"></a>函数柯里化</h3><p>函数柯里化：闭包保存参数，每轮检查，递归调用</p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-01.png" alt="面经图 1"></p><p><img src="/images/%E6%9A%91%E6%9C%9F%E5%AE%9E%E4%B9%A0%E9%9D%A2%E7%BB%8F/%E9%9D%A2%E7%BB%8F-02.png" alt="面经图 2"></p><h2 id="怎么学习、数学与场景"><a href="#怎么学习、数学与场景" class="headerlink" title="怎么学习、数学与场景"></a>怎么学习、数学与场景</h2><p>怎么学习前后端知识的 √</p><p><strong>数学能力：</strong></p><p>52张扑克牌，从中抽出两张抽到一对的概率是？√ 1&#x2F;17（先随便抽一张，然后3&#x2F;51）</p><p><strong>场景题：</strong></p><p>设计一个适合研究生日常的Agent √</p><p>从“痛点”到“功能”再到“技术实现”：</p><p><strong>核心痛点</strong>：信息过载、流程繁琐、多任务并行、缺乏系统性指导</p><p><strong>框架</strong>：对话式交互 + 主动任务执行（底层模型+工具调用）</p><p><strong>功能</strong>：文献管家&#x2F;实验助手&#x2F;写作搭档&#x2F;日程与提醒</p>]]></content>
      
      
      
        <tags>
            
            <tag> 面经 </tag>
            
            <tag> 暑期实习 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
